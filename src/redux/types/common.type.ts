export type OptionType = {
  id: any;
  name: string;
};

export type CreatePageMode = 'create' | 'update';

export type ImageType = {
  src: string;
  alt: string;
  videoId?: string;
  link?: string;
};
